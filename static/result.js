cities_result = [];
var global_input = "";
let chosen_city=0;
function handleCitiesData(citiesData) {
    citiesData.forEach(function(city) {
        cities_result.push(city);
    });
    console.log(cities_result);
    // when loaded
    // get the images from each city
    // get the label for each city
    //put the reason in the div text
}

function goToKayak(){
    //redirect to link in kayak with chosen city airport
}

